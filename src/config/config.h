#ifndef CONFIG_H
#define CONFIG_H

// Function declarations related to configuration
void initialize_config(void);
int read_config(const char *filename);
int write_config(const char *filename);

#endif // CONFIG_H

     
